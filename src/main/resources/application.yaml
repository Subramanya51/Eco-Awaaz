#spring:
#  application:
#    name: Eco-Awaaz
#
#
#
#  datasource:
#    url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres
#    username: postgres.gtjtytgiwycbhvfcuelc
#    password: "10diseISop@0"
#    driver-class-name: org.postgresql.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
spring:
  application:
    name: Eco-Awaaz

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: ${PORT:8080}